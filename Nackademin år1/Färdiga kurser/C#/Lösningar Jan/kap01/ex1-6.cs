 // Skjut in följande 4 rader före den rad där variabeln pi deklareras:

     while (r <= 0) {
       indata = InputBox.Show("Felaktigt värde! Ange ny radie!");
       r = double.Parse(indata);
     } 
